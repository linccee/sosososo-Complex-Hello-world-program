package com.overengineered.aggregator.model;

/**
 * The GeographicalScope enum represents different geographical scopes.
 * This is used to specify the scope of the greeting.
 */
public enum GeographicalScope {
    GLOBAL,
    CONTINENTAL, 
    REGIONAL,
    NATIONAL,
    LOCAL
}
